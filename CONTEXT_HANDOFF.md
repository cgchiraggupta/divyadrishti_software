TASK: Continue the Divya Drishti assistive-glasses project in `/Users/apple/Documents/divyadrishti`. Immediate priority: close out Phase 1. Two live blockers stand between "code complete" and "Phase 1 exited" — a Gemini API quota error and the phone being unplugged (latest APK not installed). Chandar orchestrates via Claude in chat; actual code/hardware execution happens through Cursor.

CURRENT STATE:
- Phase 1 is code-complete but NOT demo-verified. Static code review says done; a live Cursor run today caught two real blockers that code review couldn't see.
- **Blocker 1 — Gemini quota**: `gemini_describe.py` calls are returning `HTTP 429` (quota exceeded), confirmed live on the Pi. This blocks on-demand Describe/Read (A6) and obstacle-naming enrichment (A7). Likely a free-tier limit or billing issue, not a code bug. Fix = check Google AI Studio / Cloud Console billing on the key, then re-verify via `gemini_describe_probe.py` on the Pi (log to a temp file, don't trust SSH echo — it truncated the response once before).
- **Blocker 2 — Phone unplugged**: latest APK isn't installed on the test phone (ADB serial `0015935A7000905`), so Android-side verification (A8: pairing/Dashboard/History/Settings) is only "mostly done," not confirmed. Fix = plug in, `adb devices`, `npm run android:build`, install.
- **Everything else is confirmed live and working**, not just present in code: dual ToF obstacle detect, vibration urgency patterns, 1.0–2.5m range synced to hardware, pause/resume from phone, instant obstacle photo → History, portable wearable walk, no crash during a short walk.
- **Ahead of Phase 1 scope already**: `describe_obstacle()` in `gemini_describe.py` (separate 6s cooldown from the Describe button, event-triggered Hinglish obstacle narration) is built and wired into the main script's obstacle-alert path, and the app has `obstacleHistory.js` + obstacle cards showing this. This belongs to the future enrichment backlog, not Phase 1 — don't treat it as owed work, it's a bonus.
- **Speaker issue — status unclear, needs a decision**: `/boot/firmware/config.txt` has `hifiberry-dac` commented out (dated today) leaving only `googlevoicehat-soundcard` active, addressing the dual-I²S-driver conflict theory. But there's no logged confirmation the clean-file playback test (`aplay Front_Center.wav`) actually ran and sounded clear. Two acceptable paths, Chandar's call: (A) actually run that test and confirm, glasses speaker becomes a secondary output if clear; or (B) formally accept phone TTS (Sarvam) as the primary Phase 1 audio output and stop chasing the glasses speaker until the hardware swap.
- Full Phase 1 exit checklist (A/B/C/D tiers) and tomorrow's suggested step order are in `TOMORROW_PLAN.md` in the repo root — read that first, it's more detailed than this summary.
- App-side (React/Capacitor) is fully wired for describe: Dashboard has a "Describe / read what's ahead" button → `sendNearbyDescribe` → `/v1/command {describe}` → Gemini → text shown + Sarvam speaks it + image thumbnail shown. `DeviceContext.jsx` also auto-surfaces obstacle Gemini alerts into History (not auto-spoken, to avoid double-speaking with the Describe button).
- None of today's describe/obstacle work is committed to git yet: `gemini_describe.py`, `gemini_describe_probe.py`, `apply_describe_patch.py`, `apply_obstacle_phone_patch.py`, `sarvamTts.js`, `obstacleHistory.js`, and the Dashboard button changes are all untracked. Worth a clean patch-staged commit once Phase 1 is confirmed exited.

KEY CONTEXT:
- Pi SSH: `pi@fe80::9a91:3fc0:9b08:14%en0` (IPv6 link-local, reliable) or `pi@divyadrishti.local` (same-network only).
- Never edit `/home/pi/divya_drishti_final.py` without showing the exact diff and getting approval first. Never stop/restart `divyadrishti-sensing.service`, activate speaker/motors, or reboot the Pi without telling Chandar and getting explicit moment-of go-ahead — reading/inspecting via SSH (cat, grep, systemctl is-active) is fine without asking, but anything that touches state is not.
- Never deploy `divya_drishti_final.RECONSTRUCTED.py`, `recovered-divya_drishti_final.partial.py.txt`, or any other recovered/partial script sitting in `setup/hardware-integration/`. Never use `gpio-poweroff`. Never expose Wi-Fi passwords, pairing codes, API keys, or SSH key material in chat/logs.
- Gemini API key: lives in `~/.divyadrishti/gemini.env` on the Pi or as an env var — never pasted into chat, never echoed even if it appears in a log.
- Git hygiene: no `git add .`/`-A` ever; only stage task-relevant files/hunks (patch-stage or manual index construction when files mix relevant + unrelated changes, verify isolated result via temp worktree + lint before committing); no push unless asked. Working tree has many long-standing unrelated uncommitted changes (Dashboard/Diagnostics/Settings/History redesigns, BLE files, Supabase schema, `.env.example`, hotspot scripts) — never touch these.
- Product decisions locked: obstacle range capped 1.0–2.5m (rejected extending to 4-5m); urgent vibration alerts never drop below 85%; Gemini describe feature is on-demand/phone-triggered only (plus the new event-triggered obstacle narration with its own cooldown), never continuous, never affects the ToF/vibration main loop.
- Always show real evidence before claiming success — don't invent missing evidence. The speaker "fix" is a config change, not a confirmed fix, until the playback test result is actually seen.
- App is called "DeepGear TST" in user-facing discussion; project/repo is Divya Drishti. Chandar communicates casually/Hinglish, wants direct execution, but always wants explicit in-the-moment approval before hardware activation.

WHAT TO IGNORE:
- GPIO25 button implementation, dual-motor vibration internals, ToF sensor code, settings sync, pause/resume — all complete, stable, live-verified. Not active work.
- The old glasses-speaker-blocks-everything framing from earlier sessions — superseded by the phone-first pivot; glasses speaker is now optional/secondary pending Chandar's Step 4 decision, not a hard blocker.
- Old temporary Wi-Fi/hotspot state from BLE testing — phone/Pi should be on normal home Wi-Fi.
- Old pairing codes seen in historical debug logs — never reuse.
- The stale "mDNS discovery is broken" theory — resolved, was an Android background/doze issue.
- Legacy hotspot HTTP provisioning bug — known, deprioritized, not being worked on.
- Phase 2/3/enrichment ideas (wake word, color ID, GPS, stronger OpenCV fallback, battery %, casing/PCB/manufacturing) — explicitly out of scope until Phase 1 is formally exited. `describe_obstacle()` already existing is a bonus, not a reason to start building more enrichment now.
